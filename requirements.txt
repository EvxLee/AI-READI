# Install with:  pip install -r requirements.txt
# The final line installs src/aireadi as an editable package, so
# `from aireadi import cohort` works from anywhere in the repo.

# Azure connectivity
azure-storage-blob>=12.19.0
python-dotenv>=1.0.0

# Data analysis
# pandas is capped below 3.0 deliberately. 3.0 changes default dtypes
# (notably a dedicated str dtype) and copy-on-write semantics. With results
# freezing 26 Aug 2026, everyone reproducing an analysis should be on the
# same major version. Revisit after both papers are submitted.
pandas>=2.0.0,<3.0
numpy>=1.24.0
scipy>=1.11.0
statsmodels>=0.14.0
scikit-learn>=1.3.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Notebooks
jupyter>=1.0.0
ipykernel>=6.25.0
nbstripout>=0.6.1          # clears outputs before commit — a compliance control

# File formats
pyarrow>=12.0.0
wfdb>=4.1.0                # ECG waveforms (.hea / .dat)

# Utilities
tqdm>=4.65.0

-e .

# Testing
pytest>=7.4.0
